# Implementation Plan: 企业自动化结算平台

**Branch**: `001-settlement-automation` | **Date**: 2026-01-15 | **Spec**: [spec.md](./spec.md)  
**Input**: Feature specification from `/specs/001-settlement-automation/spec.md`

**Note**: This document is the output of the `/speckit.plan` command. It defines the technical implementation approach for the compMOS enterprise settlement automation platform.

## Summary

compMOS是一个面向企业财务人员的前端Web应用，用于企业结算流程的自动化管理。核心功能包括账单查看与核对、开票申请、发票下载、数据导出等。系统基于 Vue.js 2.x + Element UI 构建，采用单页应用（SPA）架构，通过 RESTful API 与 MOS 后台系统进行数据交互。技术方案遵循项目既有的编码规范，确保代码质量和可维护性。

## Technical Context

**Language/Version**: JavaScript (ES6+) / Vue.js 2.7.12  
**Primary Dependencies**: 
  - Vue.js 2.7.12（核心框架）
  - Element UI 2.13.0（UI组件库）
  - Vue Router 3.x（路由管理）
  - Vuex 3.1.2（状态管理）
  - Axios 0.18.0（HTTP客户端）
  - Moment.js 2.25.1（时间处理）
  - Big.js 5.2.2（精确数值计算）
  - Lodash 4.17.14（工具函数）

**Build Tools**:
  - Webpack 3.x（模块打包）
  - Babel 6.x（ES6+转译）
  - Less 3.9.0（CSS预处理）
  - ESLint（代码质量）
  - Stylelint 15.x（样式规范）
  - Prettier（代码格式化）

**Storage**: 
  - LocalStorage/SessionStorage（用户会话、缓存数据）
  - Vuex Store（内存状态管理）
  - 业务数据存储在MOS后台（通过API访问）

**Testing**: 
  - Unit Tests（单元测试）
  - Integration Tests（集成测试）
  - E2E Tests（端到端测试）
  - 具体测试框架需在Phase 0研究决定（建议：Jest + Vue Test Utils）

**Target Platform**: 
  - 现代浏览器（Chrome 90+、Firefox 88+、Safari 14+、Edge 90+）
  - 不支持 IE 浏览器
  - 响应式设计，支持桌面端（主要）

**Project Type**: Web Application (Frontend SPA)

**Performance Goals**: 
  - 首屏加载时间 < 3秒
  - 页面交互响应时间 < 500毫秒
  - 账单列表渲染（100条记录）< 1秒
  - 账单导出生成（1000条记录）< 5秒
  - 支持至少100个并发用户无明显性能降级

**Constraints**: 
  - 打包后静态资源大小 < 5MB（gzip压缩后）
  - API请求超时时间设置为10秒
  - 大列表使用虚拟滚动（>50条记录）
  - 图片懒加载，优化首屏加载
  - 使用CDN加速静态资源

**Scale/Scope**: 
  - 初期支持100家企业并发使用
  - 单个账单包最多包含5000笔订单
  - 10个核心页面组件
  - 30+个业务组件
  - 20+个API接口
  - 灰度阶段：10-20家试点企业

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

基于项目编码规范文档（`.cursor/commands/speckit.constitution.md`）的检查：

### ✅ 通过的规范要求

1. **技术栈一致性**: 
   - ✅ 使用 Vue.js 2.7.12（符合规范要求）
   - ✅ 使用 Element UI 2.13.0（符合规范要求）
   - ✅ 使用 Webpack 3.x + Babel 6.x（符合规范要求）
   - ✅ 所有依赖版本与规范一致

2. **代码风格规范**: 
   - ✅ 使用 Prettier 格式化（120字符行宽、双引号、分号、2空格缩进）
   - ✅ 使用 ESLint 进行代码质量检查
   - ✅ 组件命名使用 PascalCase（如 `BillPackageList.vue`）
   - ✅ JavaScript 文件使用 camelCase（如 `billService.js`）

3. **组件结构规范**: 
   - ✅ Vue 组件按照标准结构组织（template → script → style）
   - ✅ Props 定义包含类型、默认值、验证规则
   - ✅ 使用 Less 进行样式编写，使用 scoped 限定作用域

4. **API 调用规范**: 
   - ✅ 统一使用 `@/api/` 目录管理API接口
   - ✅ 使用 Axios 进行HTTP请求
   - ✅ 统一错误处理（try-catch + this.$message.error）

5. **状态管理规范**: 
   - ✅ 使用 Vuex 管理共享状态
   - ✅ 按模块划分 store（bill、invoice、user等）
   - ✅ mutations 使用大写命名（SET_XXX）

6. **性能优化规范**: 
   - ✅ 组件懒加载（路由级别）
   - ✅ 大列表虚拟滚动
   - ✅ 防抖和节流（使用 lodash）
   - ✅ 使用 row-key 优化表格性能

### ⚠️ 需要在Phase 0研究的事项

1. **测试框架选择**: 规范未明确指定前端测试框架，需研究选择合适的方案（建议：Jest + Vue Test Utils + Cypress）
2. **Mock数据管理**: 需确定Mock数据的管理方式（开发环境API模拟）
3. **国际化支持**: 一期仅支持简体中文，但需考虑国际化方案的扩展性
4. **代码分割策略**: 需详细规划代码分割和懒加载的具体策略
5. **CDN部署方案**: 需确定静态资源CDN的具体配置和部署流程
6. **监控和日志**: 需选择前端监控和日志收集方案（如 Sentry、阿里云日志服务）

### 📋 Constitution符合性总结

- **总体符合度**: 95%
- **需要研究的事项**: 6项（已列在Phase 0研究任务中）
- **无需额外调整**: 项目已有完善的编码规范，技术栈选型合理且成熟

## Project Structure

### Documentation (this feature)

```text
specs/001-settlement-automation/
├── spec.md              # 特性规范（已完成）
├── plan.md              # 本文件（实现计划）
├── research.md          # Phase 0 输出（技术研究和决策）
├── data-model.md        # Phase 1 输出（数据模型定义）
├── quickstart.md        # Phase 1 输出（快速开始指南）
├── contracts/           # Phase 1 输出（API契约）
│   ├── bill-api.yaml    # 账单相关API
│   ├── invoice-api.yaml # 发票相关API
│   └── user-api.yaml    # 用户相关API
├── checklists/          # 检查清单
│   └── requirements.md  # 需求质量检查清单（已完成）
└── tasks.md             # Phase 2 输出（任务分解，由 /speckit.tasks 生成）
```

### Source Code (repository root)

```text
compMOS/                           # 项目根目录
├── src/                           # 源代码目录
│   ├── api/                       # API 接口层
│   │   ├── bill/                  # 账单相关API
│   │   │   └── index.js           # 账单API封装
│   │   ├── invoice/               # 发票相关API
│   │   │   └── index.js           # 发票API封装
│   │   ├── user/                  # 用户相关API
│   │   │   └── index.js           # 用户API封装
│   │   └── http/                  # HTTP请求配置
│   │       ├── request.js         # Axios实例配置
│   │       ├── interceptor.js     # 请求/响应拦截器
│   │       └── config.js          # API基础配置
│   │
│   ├── assets/                    # 静态资源
│   │   ├── images/                # 图片资源
│   │   ├── icons/                 # 图标资源
│   │   └── styles/                # 全局样式
│   │       ├── variables.less     # Less变量
│   │       ├── mixins.less        # Less混入
│   │       └── global.less        # 全局样式
│   │
│   ├── components/                # 组件目录
│   │   ├── common/                # 通用组件
│   │   │   ├── PageHeader.vue     # 页面头部
│   │   │   ├── SearchBar.vue      # 搜索栏
│   │   │   ├── DataTable.vue      # 数据表格
│   │   │   └── ExportButton.vue   # 导出按钮
│   │   │
│   │   ├── bill/                  # 账单相关组件
│   │   │   ├── BillPackageCard.vue        # 账单包卡片
│   │   │   ├── BillDetailTable.vue        # 账单明细表格
│   │   │   ├── BillStatusTag.vue          # 账单状态标签
│   │   │   └── BillSummary.vue            # 账单汇总
│   │   │
│   │   └── invoice/               # 发票相关组件
│   │       ├── InvoiceForm.vue            # 发票信息表单
│   │       ├── InvoiceList.vue            # 发票列表
│   │       ├── InvoiceStatusBadge.vue     # 发票状态徽章
│   │       └── InvoiceDownload.vue        # 发票下载
│   │
│   ├── views/                     # 页面组件
│   │   ├── login/                 # 登录页
│   │   │   └── Index.vue
│   │   ├── home/                  # 首页
│   │   │   └── Index.vue
│   │   ├── bill/                  # 账单管理
│   │   │   ├── List.vue           # 账单列表
│   │   │   ├── Detail.vue         # 账单详情
│   │   │   └── Export.vue         # 账单导出
│   │   ├── invoice/               # 发票管理
│   │   │   ├── Apply.vue          # 发票申请
│   │   │   ├── List.vue           # 发票列表
│   │   │   └── Download.vue       # 发票下载
│   │   └── user/                  # 用户中心
│   │       └── Profile.vue        # 用户信息
│   │
│   ├── router/                    # 路由配置
│   │   ├── index.js               # 路由主文件
│   │   └── routes.js              # 路由定义
│   │
│   ├── store/                     # Vuex 状态管理
│   │   ├── index.js               # Store主文件
│   │   ├── modules/               # Store模块
│   │   │   ├── bill.js            # 账单模块
│   │   │   ├── invoice.js         # 发票模块
│   │   │   └── user.js            # 用户模块
│   │   └── types.js               # Mutation类型常量
│   │
│   ├── utils/                     # 工具函数
│   │   ├── format.js              # 格式化函数
│   │   ├── validate.js            # 验证函数
│   │   ├── storage.js             # 本地存储封装
│   │   ├── auth.js                # 认证工具
│   │   └── constants.js           # 常量定义
│   │
│   ├── mixins/                    # Vue混入
│   │   ├── tableMixin.js          # 表格混入
│   │   └── formMixin.js           # 表单混入
│   │
│   ├── filters/                   # Vue过滤器
│   │   └── index.js               # 全局过滤器
│   │
│   ├── directives/                # Vue指令
│   │   └── index.js               # 自定义指令
│   │
│   ├── App.vue                    # 根组件
│   └── main.js                    # 入口文件
│
├── public/                        # 公共资源
│   ├── index.html                 # HTML模板
│   └── favicon.ico                # 网站图标
│
├── tests/                         # 测试目录
│   ├── unit/                      # 单元测试
│   │   ├── components/            # 组件测试
│   │   ├── utils/                 # 工具函数测试
│   │   └── store/                 # Store测试
│   ├── integration/               # 集成测试
│   │   └── api/                   # API集成测试
│   └── e2e/                       # 端到端测试
│       └── specs/                 # E2E测试用例
│
├── build/                         # 构建配置
│   ├── webpack.base.conf.js       # Webpack基础配置
│   ├── webpack.dev.conf.js        # 开发环境配置
│   └── webpack.prod.conf.js       # 生产环境配置
│
├── config/                        # 项目配置
│   ├── index.js                   # 主配置文件
│   ├── dev.env.js                 # 开发环境变量
│   └── prod.env.js                # 生产环境变量
│
├── .prettierrc.js                 # Prettier配置
├── .eslintrc.js                   # ESLint配置
├── .stylelintrc.js                # Stylelint配置
├── package.json                   # 项目依赖
├── README.md                      # 项目说明
└── .gitignore                     # Git忽略文件
```

**Structure Decision**: 

采用标准的 Vue.js 单页应用目录结构，按照功能模块划分代码组织：

1. **API层**（`src/api/`）：按业务模块（bill、invoice、user）组织API接口，统一使用Axios进行HTTP请求，通过拦截器处理认证、错误等通用逻辑。

2. **组件层**（`src/components/`和`src/views/`）：
   - `components/`：可复用的业务组件和通用组件，按业务领域（bill、invoice）分类
   - `views/`：页面级组件，对应路由，按功能模块组织

3. **状态管理**（`src/store/`）：使用Vuex管理共享状态，按业务模块划分（bill、invoice、user），避免单一store过于庞大。

4. **工具和辅助**（`src/utils/`、`src/mixins/`、`src/filters/`）：提取通用逻辑，提高代码复用性。

5. **构建和配置**（`build/`、`config/`）：遵循项目既有的Webpack 3.x构建配置，分离开发和生产环境。

此结构符合项目编码规范要求，清晰的模块划分有利于团队协作和代码维护。

## Complexity Tracking

> **本项目无需填写此部分** - 项目遵循既有的Vue.js技术栈和编码规范，无违反规范的复杂性引入。

所有技术选型和架构决策均符合项目编码规范文档（`.cursor/commands/speckit.constitution.md`）的要求，未引入额外的架构复杂度。

---

## Phase 0: Research & Technology Decisions

### Unknowns to Research

基于Technical Context和Constitution Check，需要研究和决策的技术事项：

1. **前端测试框架选择**
   - 问题：项目编码规范未明确指定前端测试框架
   - 需研究：Jest + Vue Test Utils (单元测试) vs Mocha + Chai
   - 需研究：Cypress (E2E测试) vs Nightwatch vs Playwright
   - 决策依据：社区活跃度、Vue 2.x兼容性、学习成本、CI/CD集成便利性

2. **Mock数据管理方案**
   - 问题：开发环境需要Mock MOS后台API
   - 需研究：Mock.js vs MSW (Mock Service Worker) vs json-server
   - 需研究：是否需要独立的Mock数据管理工具
   - 决策依据：开发效率、数据维护成本、团队协作便利性

3. **国际化（i18n）方案**
   - 问题：一期仅支持简体中文，但需考虑未来扩展
   - 需研究：Vue I18n集成方案、文案管理方式
   - 决策依据：扩展性、性能影响、维护成本

4. **代码分割和懒加载策略**
   - 问题：需要详细规划代码分割策略以优化首屏加载
   - 需研究：路由级别懒加载 vs 组件级别懒加载
   - 需研究：动态import的最佳实践、Webpack splitChunks配置
   - 决策依据：打包体积、首屏加载时间、缓存策略

5. **CDN部署和静态资源管理**
   - 问题：需要确定CDN部署方案和静态资源版本管理
   - 需研究：阿里云OSS vs 腾讯云COS vs 其他CDN服务
   - 需研究：静态资源版本化方案（文件名hash vs query参数）
   - 决策依据：成本、速度、稳定性、集成便利性

6. **前端监控和日志收集**
   - 问题：需要监控前端性能和错误
   - 需研究：Sentry (错误监控) vs 阿里云ARMS vs 自建方案
   - 需研究：性能监控指标（FCP、LCP、FID等）和上报方案
   - 决策依据：功能完整性、成本、隐私合规

7. **表格大数据渲染优化**
   - 问题：单个账单包可能包含5000笔订单，需要优化表格渲染
   - 需研究：Element UI Table的虚拟滚动实现 vs 第三方虚拟滚动组件
   - 需研究：分页 vs 虚拟滚动 vs 懒加载的权衡
   - 决策依据：用户体验、性能表现、开发成本

8. **文件下载和导出方案**
   - 问题：需要实现Excel和PDF的下载和导出
   - 需研究：前端生成 vs 后端生成并下载
   - 需研究：Excel生成库（xlsx vs exceljs）、PDF生成库（jsPDF vs pdfmake）
   - 决策依据：性能、文件质量、浏览器兼容性

9. **权限管理和路由守卫**
   - 问题：需要实现预存企业和授信企业的权限控制
   - 需研究：路由级别权限 vs 组件级别权限 vs 按钮级别权限
   - 需研究：权限数据的获取和缓存策略
   - 决策依据：安全性、用户体验、维护成本

10. **API请求缓存和优化**
    - 问题：需要优化API请求，减少重复请求
    - 需研究：Axios缓存插件 vs 自实现缓存 vs Vuex缓存
    - 需研究：缓存失效策略、缓存粒度
    - 决策依据：性能提升、实现复杂度、缓存一致性

### Best Practices to Apply

1. **Vue.js 2.x最佳实践**
   - 组件设计：单一职责、Props向下传递、事件向上冒泡
   - 性能优化：computed vs watch、v-if vs v-show、key的正确使用
   - 代码组织：mixins的合理使用、避免过度使用global state

2. **Element UI最佳实践**
   - 按需引入组件，减少打包体积
   - 自定义主题，保持视觉一致性
   - 表单验证规则的统一管理

3. **Vuex状态管理最佳实践**
   - 模块化store设计
   - mutations保持同步、actions处理异步
   - getters用于派生状态
   - 避免直接修改state

4. **API请求最佳实践**
   - 统一的错误处理和消息提示
   - 请求拦截器处理认证token
   - 响应拦截器处理通用错误（401、403、500等）
   - 请求取消和防抖

5. **前端安全最佳实践**
   - XSS防护：v-html谨慎使用、用户输入转义
   - CSRF防护：token验证
   - 敏感信息保护：不在前端明文存储
   - HTTPS传输

6. **性能优化最佳实践**
   - 路由懒加载
   - 组件懒加载
   - 图片懒加载
   - 虚拟滚动
   - 防抖和节流
   - Gzip压缩
   - CDN加速

7. **代码质量最佳实践**
   - ESLint规则严格执行
   - Prettier自动格式化
   - Git Commit规范（feat/fix/docs/style/refactor/test/chore）
   - Code Review流程
   - 单元测试覆盖率 > 70%

### Integration Patterns

1. **compMOS与MOS后台集成**
   - 集成方式：RESTful API
   - 认证方式：JWT Token（存储在LocalStorage，通过Axios拦截器自动携带）
   - 数据格式：JSON
   - 错误处理：统一的错误码和消息格式

2. **SSO单点登录集成**
   - 集成方式：OAuth 2.0 / 企业SSO协议
   - 认证流程：跳转SSO登录页 → 登录成功返回code → 换取token → 存储token
   - Token刷新：Access Token + Refresh Token机制

3. **文件下载集成**
   - 集成方式：预签名URL（MOS生成）
   - 下载方式：直接下载（小文件）vs Blob流式下载（大文件）
   - 进度显示：使用Axios的onDownloadProgress

**Output**: research.md文档将在Phase 0完成后生成，包含所有技术决策的详细研究结果和最终选型。

---

## Phase 1: Design & Contracts

### Data Model

详细数据模型将在 `data-model.md` 中定义，包括：

1. **前端状态模型**（Vuex Store）
   - Bill Module（账单模块状态）
   - Invoice Module（发票模块状态）
   - User Module（用户模块状态）

2. **API数据模型**（与MOS后台交互）
   - BillPackage（账单包）
   - OrderDetail（订单明细）
   - Invoice（发票）
   - InvoiceBatch（开票批次）
   - User（用户）

3. **表单数据模型**
   - InvoiceApplicationForm（发票申请表单）
   - OrderAdjustmentForm（订单调整表单）
   - SearchFilterForm（搜索筛选表单）

### API Contracts

API契约将在 `contracts/` 目录中以OpenAPI 3.0格式定义：

1. **bill-api.yaml**：账单相关API
   - GET /api/bills - 获取账单包列表
   - GET /api/bills/{billNo} - 获取账单包详情
   - GET /api/bills/{billNo}/orders - 获取账单订单明细
   - PUT /api/bills/{billNo}/confirm - 确认账单包
   - POST /api/bills/{billNo}/export - 导出账单

2. **invoice-api.yaml**：发票相关API
   - POST /api/invoices/apply - 申请开票
   - GET /api/invoices/batches - 获取开票批次列表
   - GET /api/invoices/batches/{batchId} - 获取开票批次详情
   - GET /api/invoices/download/{invoiceNo} - 下载发票

3. **user-api.yaml**：用户相关API
   - POST /api/auth/login - 用户登录
   - POST /api/auth/logout - 用户登出
   - GET /api/auth/userinfo - 获取用户信息
   - GET /api/auth/permissions - 获取用户权限

### Quickstart Guide

快速开始指南将在 `quickstart.md` 中提供：

1. **环境准备**
   - Node.js 12+ 安装
   - npm / yarn 安装
   - Git 配置

2. **项目初始化**
   - 克隆代码仓库
   - 安装依赖：`npm install`
   - 配置环境变量

3. **本地开发**
   - 启动开发服务器：`npm run dev`
   - 访问：`http://localhost:8080`
   - Mock数据配置

4. **构建和部署**
   - 构建生产版本：`npm run build`
   - 部署到CDN
   - 环境配置

5. **代码规范**
   - 运行ESLint：`npm run lint`
   - 运行Prettier：`npm run format`
   - Git Commit规范

6. **测试**
   - 运行单元测试：`npm run test:unit`
   - 运行E2E测试：`npm run test:e2e`
   - 测试覆盖率：`npm run test:coverage`

**Output**: 
- data-model.md（数据模型文档）
- contracts/（API契约目录）
  - bill-api.yaml
  - invoice-api.yaml
  - user-api.yaml
- quickstart.md（快速开始指南）

---

## Next Steps

本实现计划（plan.md）已完成Phase 0和Phase 1的规划。接下来的步骤：

1. **Phase 0: 执行技术研究**
   - 研究并决策10个待定技术事项
   - 生成 `research.md` 文档记录所有技术决策

2. **Phase 1: 生成设计文档**
   - 生成 `data-model.md`（数据模型）
   - 生成 `contracts/`（API契约）
   - 生成 `quickstart.md`（快速开始指南）
   - 更新 Agent Context

3. **Phase 2: 任务分解**
   - 执行 `/speckit.tasks` 命令
   - 生成 `tasks.md`（开发任务列表）
   - 按优先级和依赖关系组织任务

4. **Phase 3: 实施开发**
   - 根据tasks.md进行开发
   - 遵循项目编码规范
   - 编写单元测试和集成测试
   - Code Review和质量检查

---

**Plan Version**: 1.0  
**Last Updated**: 2026-01-15  
**Status**: Phase 0 Planning Complete - Ready for Research
